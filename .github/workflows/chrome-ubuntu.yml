name: CI (2)

on:
  push:
    branches: [master]
  pull_request:
    branches: [master]

jobs:
  test:
    uses: ./.github/workflows/browser-integration.yml
    with:
      os: ubuntu-latest
      browser: chrome
