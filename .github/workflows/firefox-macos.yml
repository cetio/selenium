name: CI (4)

on:
  push:
    branches: [master]
  pull_request:
    branches: [master]

jobs:
  test:
    uses: ./.github/workflows/browser-integration.yml
    with:
      os: macos-latest
      browser: firefox
