name: CI (1)

on:
  push:
    branches: [master]
  pull_request:
    branches: [master]

jobs:
  test:
    uses: ./.github/workflows/browser-integration-tests.yml
    with:
      os: macos-latest
      browser: chrome
